-- import database into docker 
docker exec -i phpbeginner-mariadb-1  mariadb -u admin -psaboor123 school < table.sql

failed to get console mode for stdin: The handle is invalid.
```
The code goes here... 
docker ps 
```
docker compose up -d
docker compose up  --build
docker-compose build --no-cache --pull
docker-compose build --no-cache

# delete 
docker system prune
docker system prune –a
docker volume ls
docker volume rm [volume_name] 
docker volume ls -f dangling=true 
docker images –a

docker system prune -a --volumes
# To delete all containers including its volumes use,
docker rm -vf $(docker ps -aq)
# To delete all the images,
docker rmi -f $(docker images -aq)
# delete everything 
docker system prune -a --volumes
--
htmlspecialchars convert HTML into plaintext
strip_tags  remove HTML from page

add functions on composer.json
"files": [
    "app/Config/Routes.php"
]

This is '...' is separate operator

